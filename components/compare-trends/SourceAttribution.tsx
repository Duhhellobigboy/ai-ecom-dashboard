export default function SourceAttribution() {
  return (
    <p className="text-[11px] text-zinc-600 text-center sm:text-left">
      Data from Google Trends via SerpAPI · Relative interest scores (0–100), not absolute search volume.
    </p>
  );
}
